@config
Feature: config override: pending TODO in specs

  1st story pending TODOs

  - describe limit in terms of number of overrides and total size of overrides.
  - behavior in text mode when overriding more than once the same config: not detected, last wins.
  - illustrate invalid key behavior in text mode:
      key without matching config set label
      config set label with incorrect characters #, = spaces

  next stories pending TODOs

  - how the UI scales with lots of properties: filter, sort
